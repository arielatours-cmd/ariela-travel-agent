# Ariella QA — v9.7.107
PASS:
- DB match pinning accepts both `offer_id` and database `id`.
- Fix covers initial DB match, alternative buttons, fresh trip results, and account rendering.
- Regression target: Sofia October -> same destination/other dates -> November Sofia in 48h DB -> display with 0 API calls.
- Python compilation and Jinja parsing passed.
